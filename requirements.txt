# Pinned to the exact versions the jev-local models were trained with.
# joblib artifacts are version-sensitive: train and serve on the same stack.
scikit-learn==1.6.1
numpy==2.2.2
scipy==1.14.1
joblib==1.4.2
